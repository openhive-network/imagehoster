machine:
    services:
        - docker

dependencies:
    override:
        - echo "Ignore CircleCI detected dependencies"

test:
    override:
        - docker build -t steemit/imagehoster ./imagehoster && docker run steemit/imagehoster npm test


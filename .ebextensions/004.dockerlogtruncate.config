commands:
    add_docker_log_truncate_cronjob:
        command: "crontab -l | grep -q 'docker' || (crontab -l ; echo '0 * */6 * * /bin/echo 0 | tee /var/lib/docker/containers/*/*.log')| crontab -"
        ignoreErrors: true
